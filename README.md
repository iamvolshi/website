# VOLSHi Website

Static corporate landing site for VOLSHi.

## Structure

- `index.html` — landing page
- `assets/` — supplied brand and visual assets
- `src/` — site CSS and JavaScript
- `docs/` — project documentation

No build step is required. Serve the repository root as a static site.
